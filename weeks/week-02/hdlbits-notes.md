#Solved problems + notes:
1. assign to set up the circuit
2. input, output = input wire, output wire
3. bitwise-NOT (~) vs logical-NOT (!)
4. bitwise-AND (&) vs logical-AND (&&)
5. bitwise-OR (|) vs logical (||)
6. NOR = NOT OR
7. XOR (^) => XNOR (~) + (^)
8. Track the wire and remember the shape/symbol of each logic gates to assign correctly